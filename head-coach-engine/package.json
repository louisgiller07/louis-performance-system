{
  "name": "head-coach-engine",
  "version": "0.2.0",
  "description": "Louis Performance System — Head Coach Engine (V0.2 vertical slice locale)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:edge": "npm run build && vitest run --config vitest.edge.config.ts",
    "test:m3:http": "npm run build && tsx tests/edge/http/orchestrate.ts",
    "run:example": "tsx src/cli/runExample.ts",
    "deploy:daily-run": "npm run build && npx supabase functions deploy daily-run --use-api --project-ref uvolpldwwyvadlamulvr --workdir .."
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest",
    "vitest": "latest"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.3"
  }
}
