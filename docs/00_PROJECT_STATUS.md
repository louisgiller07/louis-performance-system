# 00 — Project Status

**Dernière mise à jour :** 31 août 2026
**Version canonique en cours :** V0.2
**Phase actuelle :** **V0.3_002 — Domain Coaching Enrichment : COMPLETE (2026-08-30).** V0.3_002A CLOSED / ARCHITECTURE LOCKED (2026-08-28) → V0.3_002B (Technique DH) → V0.3_002C (Mental) → V0.3_002D (Nutrition) → V0.3_002E (intégration/régressions, test-only) → V0.3_002F (rollout remote/clôture) — les 6 jalons de la décomposition verrouillée sont désormais tous **CLOSED**. **V0.3_003 — Planning / Session Intent : IN PROGRESS (depuis 2026-08-31), V0.3_003A CLOSED / ARCHITECTURE LOCKED, V0.3_003B (data-access/write path) CLOSED LOCALLY, V0.3_003C (web planning workflow) CLOSED LOCALLY (2026-08-31).** `daily-run` sert `head-coach-engine@0.2.0-m1-v0.3_002d` en production sur `uvolpldwwyvadlamulvr`. V0.3_001 reste **COMPLETE (2026-08-28)** comme historique clos — voir ci-dessous. M5 reste COMPLETE, M4 reste COMPLETE (client web de production, `https://louis-performance-system.vercel.app`). M5 (longitudinal/pattern evidence/insights) a franchi M5_001 à M5_007 — tous **CLOSED**, voir `docs/11_DECISION_LOG.md` pour le détail milestone par milestone. M5_006C/M5_006D restent implémentation locale uniquement (aucune migration, réutilisent le schéma/RPC déjà déployés par M5_006A/M5_006B). M5_007 (insights déterministes + ledger de revue humaine) est **CLOSED** — le schéma/RPC de revue humaine M5_007 est déployé sur Supabase (`uvolpldwwyvadlamulvr`, 4 migrations, `local=remote=26`, `pending=0`). V0.3_001 a ensuite rendu ce pipeline utilisable de bout en bout : `refresh-longitudinal`/`get-insights` (V0.3_001A/B, orchestration + backfill remote) puis `submit-review` + surface web `/insights` (V0.3_001C) sont désormais **déployés et ACTIVE** sur `uvolpldwwyvadlamulvr`/`nalynt` — toujours aucun scheduler, aucune intégration `daily-run`. Aucun peuplement automatique de production (`pattern_insight_identities`/`pattern_insight_reviews` = 0/0 sur remote — aucun candidat naturel n'est encore apparu, aucune donnée n'a été créée pour en fabriquer un), aucun pattern appris n'influence `daily-run`, `accepted_as_insight` n'active jamais le coaching. **V0.3_001 dans son ensemble est COMPLETE.**

M3 — Edge Function HTTP exposant `runDailyFor` : **DONE (local + remote)**, `daily-run` déployée et **ACTIVE** sur `uvolpldwwyvadlamulvr` (`verify_jwt: true`). Voir `docs/11_DECISION_LOG.md` (2026-08-17 — M3_001/M3_002/M3_003 ; 2026-08-18 — M3_005/M3_006).

M2 — Connexion Supabase read/write : **DONE (local + remote)**, voir `docs/11_DECISION_LOG.md` (2026-08-16 — clôture locale ; 2026-08-17 — déploiement remote).

## Où on en est réellement

### DONE

- **Onboarding athlète complet** (11 blocs), source de vérité pour `02_ATHLETE_PROFILE.md`.
- **DB Supabase V0.2 déployée et validée** : 12 tables, enums, RLS, triggers, seed data Louis.
- **Spec Head Coach Engine V0.2 consolidée** avec tous les principes canoniques (multidimensional state, décisions causales, non-double-counting, T-X default framework, support multi-jours, SAFETY limitée, soft constraints arbitrables, douleur non-SAFETY actionnable, séparation DB/interne).
- **M1 — Vertical Slice locale du Head Coach Engine** (`head-coach-engine/src/{types,engine,rules,domains,mapping}`) : pipeline complet `RawContext → DailyPlan`, 75/75 tests verts, build TypeScript strict clean, CLI de démonstration fonctionnelle. **Verdict architecte : APPROVED (2026-08-13). Frozen** sauf bug métier réel découvert ultérieurement.
- **M2 — Connexion Supabase read/write locale : DONE (local + remote)** : développement local terminé 2026-08-16 (baseline V0.2 + migrations `M2_001`→`M2_006`, DAL/adapter, `computeDailyFor` zéro écriture et `runDailyFor` RPC `persist_daily_run` atomique, cycle longitudinal A1→A5 prouvé, équivalence fixture ↔ Supabase sur 19 scénarios canoniques, 226/226 tests verts dont 75/75 M1). Déploiement remote effectué 2026-08-17 sur `uvolpldwwyvadlamulvr` : baseline `20260814095000` enregistrée comme applied sans rejouer son SQL, `M2_001`→`M2_006` déployées via `db push`, migration history LOCAL=REMOTE, `db push --dry-run` = "Remote database is up to date", post-deploy audit PASS, données existantes préservées (`athletes`, `goals`, `training_blocks`, `race_calendar`, `athlete_baselines`, `weekly_availability` — comptages identiques avant/après). Voir `docs/11_DECISION_LOG.md` et `docs/12_BACKLOG.md`.
- **M3 — API HTTP : DONE (local + remote)** : `supabase/functions/daily-run` — `Authorization: Bearer <JWT>` → `@supabase/server@1.4.1` (`withSupabase({auth:"user"})`) → `ctx.supabase`/RLS → athlete propre → `ctx.supabaseAdmin` → `runDailyFor(admin, athlete.id, date)` (import du JS compilé `head-coach-engine/dist/supabase/runDailyFor.js`, jamais la source `.ts`) → réponse `{dailyPlan, decisionId, healthFlagId, warnings}` ou mapping d'erreur HTTP stable (422/500/403/400/405). Local : `npm test` 226/226 (dont 75/75 M1), `npm run test:edge` 9/9, `npm run test:m3:http` 26/26. Remote (`uvolpldwwyvadlamulvr`) : packaging `--use-api` prouvé (canary M3_005), `daily-run` déployée et ACTIVE, run authentifié réel complet prouvé (M3_006 — 401/422/200, persistance vérifiée, isolation RLS, aucune fuite, cleanup complet). Aucune migration, aucun changement M1/M2. Voir `docs/11_DECISION_LOG.md` (2026-08-17 — M3_001/M3_002/M3_003 ; 2026-08-18 — M3_005/M3_006).
- **M4 — première interface utilisable : COMPLETE (2026-08-19)** : client web `web/` (React 19 + Vite + TypeScript strict + Tailwind v4 + React Router 7), authentification Supabase (`AuthContext`/`RequireAuth`/`LoginPage`), `/today` (date locale, check-in, génération/rendu du `DailyPlan`), `/history` + `/history/:decisionId` (lecture seule des décisions persistées, dégradation propre sur ligne legacy), polish mobile (cibles tactiles, zoom iOS évité, navigation deux-onglets), déploiement production HTTPS sur Vercel (`https://louis-performance-system.vercel.app`, projet/scope dédiés `louis-performance-system`, jamais `graviacoach`). **Correctif de sécurité** : `public.decisions` rendue réellement append-only (policy `SELECT`-only pour `authenticated`, `REVOKE` des privilèges d'écriture directs, `persist_daily_run`/`service_role` reste l'unique chemin d'écriture). Smoke authentifié réel sur le téléphone de Louis (login, `/today`, check-in, `/history`, détail, refresh direct, logout/relogin) : **tous PASS**. Voir `docs/11_DECISION_LOG.md` (2026-08-19 — M4_006 sécurité, M4_007 déploiement) et `docs/12_BACKLOG.md` (section M4).
- **M5 — Longitudinal / Pattern Evidence / Insights : COMPLETE (2026-08-27)** : timeline longitudinale en lecture seule (`longitudinal-engine`, jamais importé par `head-coach-engine`), calculateur d'outcomes post-décision (M5_004), détecteurs déterministes recommandation-vs-exécution et sommeil-énergie et persistance-douleur (M5_005/M5_006B/M5_006C), ledger d'evidence append-only avec cycle de vie actif/retiré (M5_006A/M5_006B, déployé), agrégation déterministe d'evidence effective (M5_006D, local uniquement), projection déterministe d'insights + ledger de revue humaine append-only (M5_007, déployé — `pattern_insight_identities`/`pattern_insight_reviews`/`persist_pattern_insight_review`/2 vues sur `uvolpldwwyvadlamulvr`). Pipeline verrouillé à chaque étape : `evidence → agrégat → candidat d'insight → revue humaine`, **jamais** plus loin — aucun score de confiance/signification, aucune causalité, aucun LLM, aucune activation automatique de pattern, `accepted_as_insight` signifie uniquement acceptation humaine du texte de l'insight. Aucun scheduler, aucune Edge Function dédiée, aucune UI web, aucune intégration `daily-run` ; Safety A1-A5 inchangée, M1-M4 restent frozen. Détail milestone par milestone (M5_001A/B → M5_002A/B → M5_003 → M5_004 → M5_005 → M5_006A/B/C/D → M5_007) dans `docs/11_DECISION_LOG.md`.
- **V0.3_001A — Correction de runtime + orchestration : CLOSED LOCALLY (2026-08-28)** : implémentation (`d69c81593aff4b25ad58aef3b2ac7c703a7d1d45`, `feat: add longitudinal insight runtime orchestration`) + durcissement (`a9b50c13f312879b48d0fbe82f3776f5ad88e11b`, `fix: harden V0.3_001A runtime orchestration`) poussés sur `origin/main`. Correction de l'identité de persistance `recommendation_vs_actual_execution@1.0.0` (`evaluationKey = evidenceKey = decision:<decisionId>` pour `evidence` et `no_evidence`, retrait/réactivation via `transition_pattern_evidence_lifecycle` déjà déployée) — précondition remote read-only confirmée immédiatement avant le changement de code puis reconfirmée à la clôture par requête SQL directe : 0 identité pour `(recommendation_vs_actual_execution, 1.0.0)`, aucune ligne historique jamais réécrite. Orchestrateur `runDetectors` + `assembleAthleteTimeline` (plage compacte dérivée des données réelles, marge de lookback détecteur incluse, jamais `INSIGHT_AGGREGATION_RANGE` passée à `buildTimeline` — voir `docs/06_ARCHITECTURE.md` §V0.3_001 pour le modèle de plages complet). Deux Edge Functions implémentées et testées **localement uniquement** (aucun déploiement remote) : `refresh-longitudinal` (écriture, `POST` corps vide, lecture RLS authentifiée + écriture `service_role` uniquement, réponse `{status, processingDate, outcomes, detectors, errors}` sanitisée par détecteur) et `get-insights` (lecture, `GET` sans paramètre, client RLS authentifié uniquement, zéro `service_role`, plage statique `1900-01-01..9999-12-31`). Tests : `longitudinal-engine` 689/689, harness HTTP V0.3 30/30 (×2 consécutifs), régressions M1/M2=226/226, edge=9/9, M3 HTTP=26/26, completed-session unit=73/73/HTTP=70/70, web=242/242, builds PASS. `submit-review` et la surface web Insights restent non implémentées (V0.3_001C) ; aucun backfill remote historique exécuté (V0.3_001B) ; `daily-run` non modifié, aucune activation automatique de coaching. Voir `docs/11_DECISION_LOG.md`.
- **V0.3_001B — Backfill + durcissement complet (remote) : CLOSED REMOTE (2026-08-28)** : depuis l'implémentation runtime-aligned HEAD `27fcb564dff2558b1a1dafc81c914d46a8957011` (code identique à V0.3_001A CLOSED LOCALLY), `refresh-longitudinal` et `get-insights` déployés et **ACTIVE** sur `uvolpldwwyvadlamulvr` (`verify_jwt: true`, artefact de production identique — `ezbr_sha256` `c855e79897c23369f9cb058fc94ed9824f16b1334dcfde7fa1577f41a1fe907b` — à travers les 3 déploiements successifs, confirmant zéro dérive de packaging entre les commits tooling-only). Premier backfill remote authentifié : `status: complete`, `outcomes: {attempted:42, writeSucceeded:42, alreadyExisted:0, errorCount:0}`, les 3 détecteurs entièrement `skippedNoPrior` (14/3/3, `completed_sessions=0` rend la recommandation déterministe `no_evidence`), vérifié relationnellement en lecture seule (42 outcomes joints sans orphelin, 14 décisions sources exactement représentées, 3 horizons chacune, un seul scope athlète, zéro incohérence). Contrat d'idempotence audité depuis le code réel (`calculateAndPersistOutcomes` court-circuite via la timeline déjà chargée **avant** tout appel RPC ; `persist_decision_outcome` interdit toute `UPDATE`, rejette un contenu conflictuel sur la même identité) puis prouvé en local et confirmé par un second appel remote guardé : `outcomes: {attempted:0, writeSucceeded:0, alreadyExisted:42, errorCount:0}`, mêmes 3 détecteurs `skippedNoPrior`, ensemble des métriques agrégées et relationnelles revérifiées strictement identique à l'état post-premier-backfill. État final : `decision_outcomes=42`, `pattern_evidence_*=0/0/0/0`, `pattern_insight_*=0/0`. Outillage opérateur ajouté (`longitudinal-engine/tools/**`, jamais dans `dist/` de production, jamais importé par `supabase/functions/**`/`web/**`) : preview/canary en lecture seule + deux runners d'invocation guardée, outillage de rollout/débogage, jamais un chemin d'exécution produit. `submit-review` et la surface web Insights restent non implémentées (V0.3_001C) ; `daily-run` non modifié, aucune activation automatique de coaching, aucun scheduler ni appelant automatique déployé à ce stade. Voir `docs/11_DECISION_LOG.md`.
- **V0.3_001C — API de revue canonique + surface web Insights : CLOSED REMOTE (2026-08-28)** : `submit-review` implémenté (sélecteur de candidat `(athleteId résolu côté serveur, detectorRuleId)`, validation complète du jeton de fraîcheur 7 dimensions, `stale_candidate`/`candidate_not_found`, `candidate_snapshot` toujours généré par le serveur), durci contre des races/concurrence/isolation réelles (endpoint HTTP réel, Postgres/RPC locaux réels, jamais mockés) : insertion/inchangé/supersession, égalité DB du `candidate_snapshot` côté serveur, soumissions concurrentes identiques (exactement un `inserted` + un `unchanged`) et divergentes (exactement un `inserted` + un `superseded`, chaîne de supersession valide), isolation inter-athlète, linéarisation de fraîcheur (sémantique A) prouvée via un verrou consultatif PostgreSQL réel objectivement observé (`pg_locks`, jamais une pause arbitraire). `V0.3` HTTP = **84/84**. Surface web `/insights` minimale implémentée et revue (chargement `get-insights` uniquement, jamais `refresh-longitudinal` automatique ; corps `submit-review` construit explicitement, jamais `athleteId`/`candidateSnapshot` ; `stale_candidate`/`candidate_not_found` gérés par refetch authoritatif sans resoumission automatique) : `web` = **293/293**, build PASS. Déployé remote : `submit-review` **ACTIVE v1** `verify_jwt: true` sur `uvolpldwwyvadlamulvr` (`get-insights`/`refresh-longitudinal` inchangés) ; surface web déployée en production sur `nalynt/louis-performance-system` (`https://louis-performance-system.vercel.app`) ; lecture authentifiée remote réelle `get-insights` → `HTTP 200`, `candidateCount=0`. **Écriture de revue réussie en production : NON EXÉCUTÉE** — la production ne contenait aucun candidat courant naturel à la clôture (`pattern_evidence_*`/`pattern_insight_*` = 0 sur toute la ligne), et aucune donnée applicative synthétique n'a été créée pour fabriquer cette précondition (décision d'architecture verrouillée, sémantique B — voir `docs/06_ARCHITECTURE.md` §Discipline de rollout `submit-review` et `docs/11_DECISION_LOG.md`). `daily-run` non modifié, aucune activation automatique de coaching, aucun scheduler introduit.
- **V0.3_001 — Longitudinal Intelligence Runtime + Human Insight Review : COMPLETE (2026-08-28)** : V0.3_001A CLOSED LOCALLY, V0.3_001B CLOSED REMOTE, V0.3_001C CLOSED REMOTE — les 3 jalons de la décomposition verrouillée (`docs/11_DECISION_LOG.md`, aucun V0.3_001D). Pipeline utilisable de bout en bout : `evidence → agrégat → candidat d'insight → revue humaine`, jamais au-delà.
- **V0.3_002A — Domain Coaching Enrichment, architecture/contrats : CLOSED / ARCHITECTURE LOCKED (2026-08-28)** : décomposition verrouillée V0.3_002A (ce jalon) → V0.3_002B (Technique DH) → V0.3_002C (Mental) → V0.3_002D (Nutrition) → V0.3_002E (intégration/régressions) → V0.3_002F (rollout remote/clôture). Propriété de signal Option C verrouillée (Training reste seul propriétaire de décision des signaux mentaux RED déjà consommés par `MENTAL_RED` ; Mental s'exécute strictement après Training et ne peut que lire ce signal en support, jamais le reconsommer). Config de coaching-profil statique typée mono-athlète bornée (Option A) approuvée — non encore créée. Contrat de sortie verrouillé sur les champs `DailyPlan` déjà existants (`dh_or_technical.{active,focus,spot_hint}`, `mental.{active,focus,action_hint}`, `nutrition.{active,focus,hydration_target_l,notes}`), aucun nouveau champ, aucune migration, aucun nouveau champ `daily_checkins`, aucun changement de production web attendu. Documentation uniquement — aucun code, aucun test, aucune migration, aucun déploiement. Voir `docs/06_ARCHITECTURE.md` §V0.3_002 et `docs/11_DECISION_LOG.md`.
- **V0.3_002B — Technique DH : CLOSED LOCALLY (2026-08-28)** : implémentation (`b28e013fce8ca7f3a9896a76351c4f058c82e9fa`, `feat: add V0.3_002B technique coaching domain`) + durcissement provenance (`e9573dd4cea8a1e804e2acc2a20bd6517817fcd6`, `fix: harden V0.3_002B engine provenance`) poussés sur `origin/main`. Nouveau domaine pur `computeTechniqueDomain` peuple `dh_or_technical.{active,focus,spot_hint}` sur le chemin non-SAFETY uniquement : actif pour `DH_TECHNICAL`/`DH_PERFORMANCE`/`DH_LIGHT`/`PUMPTRACK` (séance finale post-arbitrage), focus unique approuvé ("Fixe ta ligne, dose le freinage, laisse rouler."), `spot_hint` limité à un allowlist de 4 chaînes catégorielles (aucune venue nommée). Fenêtre de proximité course C1.5 = J+1 à J+14 inclusif sur `RawContext.upcoming_races` (`TECHNIQUE_POLICY.raceProximityWindowDays`), distincte de la fenêtre `PRE_EVENT` de `computeEventContext` (inchangée à 7 jours, jamais le sélecteur C1.5). `raceCalendarRepo.ts` élargi en lecture seule pour couvrir J+14 (borne future uniquement, borne historique inchangée) — exception M2 read-only explicitement approuvée après le preflight de V0.3_002B à la frontière frozen M1-M4, strictement bornée à la disponibilité des données de course dans `RawContext`, sans modification d'aucun contrat de persistance/écriture M2 ; inertie sur `EventContext`/décision/séance finale/`triggered_rules` pour les courses J+8..J+14 prouvée par intégration réelle contre Supabase local (6 scénarios). Fatigue C1.6 = `systemic` OU `legs` OU `arms_grip` = AMBER exactement (RED seul exclu) ; aucune interaction `SignalTrace`. Config profil athlète bornée créée (`src/config/athleteCoachingProfile.ts`, un seul focus approuvé) et config de politique de domaine distincte (`src/config/techniquePolicy.ts`). `ENGINE_VERSION` porté à `head-coach-engine@0.2.0-m1-v0.3_002b` (provenance comportementale du `DailyPlan`, `package.json` inchangé à `0.2.0`). Safety/Training/`SignalTrace`/`eventContext.ts`/`raceProtocol.ts` strictement inchangés. Aucune migration, aucun nouveau champ `daily_checkins`, aucun changement web. Tests : `npm test` **275/275**, `npm run test:edge` **9/9**, build PASS. Aucun déploiement remote. Voir `docs/06_ARCHITECTURE.md` §V0.3_002 et `docs/11_DECISION_LOG.md`.
- **V0.3_002C — Mental : CLOSED LOCALLY (2026-08-29)** : implémentation (`06e878887e3e09f8737bfdc81af999977b92e9ac`, `feat: add V0.3_002C mental coaching domain`) + durcissement (`3b1753b4eabb73cdf9050fbf1a994bd1f1a72e8a`, `test: harden V0.3_002C mental ownership regressions`) poussés sur `origin/main`. Nouveau domaine `computeMentalDomain` peuple `mental.{active,focus,action_hint}` sur le chemin non-SAFETY uniquement, champs orthogonaux : `focus` déclenché uniquement par `event_context.phase === "PRE_EVENT"` (aucun filtre de priorité de course), texte "Comme à Wiriehorn." ; `action_hint` déclenché par `dimensions.mental` AMBER (`stress_high` puis `motivation_low` par précédence, exactement un signal consommé — `MENTAL_AMBER_STRESS`/`MENTAL_AMBER_MOTIVATION`) ou RED (lecture de support non consommante du signal déjà propriété de `MENTAL_RED`, zéro `consume()` par Mental). Propriété de signal Option C : Training reste seul propriétaire de décision en RED (précédence `stress_high` puis `motivation_low` inchangée, y compris dans le cas mixte stress AMBER + motivation RED où Training sélectionne malgré tout `stress_high` — comportement Training existant enregistré tel quel, non modifié par 002C). La `TriggeredRule` Mental AMBER est calculée avant, mais poussée dans le tableau partagé `triggered_rules` seulement après construction du littéral `DailyPlan`, pour ne jamais contaminer `training.objective`/`reasoning`/`override_reason` — prouvé par comparaison appariée GREEN/AMBER stricte. `event_context` POST_EVENT/RACE_DAY_GENERIC n'inhibent jamais globalement Mental (seul `focus` reste absent) ; aucun debrief déguisé, aucun coaching de course en direct. Config profil athlète étendue (`mental.preRaceCue.{id: "wiriehorn_flow_reference", cue: "Comme à Wiriehorn."}` uniquement). `ENGINE_VERSION` porté à `head-coach-engine@0.2.0-m1-v0.3_002c` (décision de provenance bornée à cette occasion, `package.json` inchangé à `0.2.0`, désormais directement testée). Safety/Training/Technique/`SignalTrace`/`eventContext.ts`/`raceProtocol.ts` strictement inchangés. Aucune migration, aucun nouveau champ `daily_checkins`, aucun changement web. Tests : `npm test` **310/310**, `npm run test:edge` **9/9**, build PASS — incluant preuve intégrée réelle (RawContext→DailyPlan) des cas both-RED, stress RED + motivation AMBER, et stress AMBER + motivation RED. Aucun déploiement remote. Voir `docs/06_ARCHITECTURE.md` §V0.3_002 et `docs/11_DECISION_LOG.md`.
- **V0.3_002D — Nutrition : CLOSED LOCALLY (2026-08-30)** : implémentation (`eac175869a8f30ddd49352c97c942ea574f5106b`, `feat: add V0.3_002D nutrition coaching domain`) + durcissement (`792608c633922aeea5464cc385382a544d1b74c4`, `fix: harden V0.3_002D nutrition provenance`) poussés sur `origin/main`. Nouveau domaine `computeNutritionDomain` peuple `nutrition.{active,focus,hydration_target_l,notes}` sur le chemin non-SAFETY uniquement : `focus` (rappel race-week) déclenché par `active_mode === "RACE_WEEK"` (distinct de `PRE_EVENT`, jamais utilisé par Nutrition) ; branche primaire (`notes`+`hydration_target_l`) à précédence déterministe explicite RACE DAY (`event_context.in_progress`, jamais `phase === "RACE_DAY_GENERIC"`) > jour DH (`final_session.kind`, séance déjà arbitrée) > séance de force planifiée (`ctx.planned_session.kind` brute — le moteur ne connaît pas l'exécution) > aucune ; les deux composantes sont orthogonales et coexistent (ex. race-week + jour DH). `hydration_target_l = 2` uniquement sur la branche force (seule cible numérique canonique unique existante, C5.3) ; la plage C5.4 (`~3-3.5 L/jour` jour DH) reste texte uniquement dans `notes`, jamais convertie en point médian ; C5.2 (60 min post-force) et C5.6 (2 h avant le premier run jour de course) restent également texte uniquement. Aucune interaction `SignalTrace`, aucune `TriggeredRule` émise — confirmé structurellement (aucune heuristique C5.x ne consomme de signal de dimension). Config `src/config/nutritionPolicy.ts` (NEW) porte les quatre constantes numériques génériques (`baselineHydrationTargetL`, `dhHydrationRangeL`, `strengthPostWindowMinutes`, `raceBreakfastLeadHours`) : c'est la source de vérité runtime réelle des quatre valeurs — `baselineHydrationTargetL` pilote le champ structuré `hydration_target_l`, les trois autres pilotent les chaînes de texte déterministes (durcissement, formatteur décimal français minimal, aucune copie numérique dupliquée, aucune extension `athleteCoachingProfile.ts`). `ENGINE_VERSION` porté à `head-coach-engine@0.2.0-m1-v0.3_002d` ; son assertion exacte n'a désormais qu'un seul propriétaire (`tests/engineVersion.test.ts`, NEW) — les copies qui existaient dans T12 et T13 ont été retirées (durcissement architectural applicable à toute future provenance bump). Safety/Training/Technique/Mental/`SignalTrace`/`eventContext.ts`/`raceProtocol.ts` strictement inchangés. Aucune migration, aucun nouveau champ `daily_checkins`, aucun changement web. Tests : `npm test` **347/347**, `npm run test:edge` **9/9**, build PASS — incluant preuve d'intégration réelle course-en-cours (`upcoming_races` → `computeEventContext` → `buildDailyPlan`, jamais un `EventContext` construit à la main), preuve Safety dominant un déclencheur Nutrition par ailleurs valide, et prévention de fuite `hydration_target_l` sur les trois combinaisons de précédence. Aucun déploiement remote. Voir `docs/06_ARCHITECTURE.md` §V0.3_002 et `docs/11_DECISION_LOG.md`.
- **V0.3_002E — Intégration/régressions cross-domaine : CLOSED LOCALLY (2026-08-30, test-only)** : `head-coach-engine/tests/t14_crossDomainIntegration.test.ts` (NEW, seul fichier créé — aucun fichier de production, aucun test historique modifié) prouve la composition des domaines Technique/Mental/Nutrition (002B/C/D) contre Safety/Training/Recovery/`EventContext`/propriété de signal, via de vrais `RawContext` passés à `buildDailyPlan` (aucune injection de dimension dérivée, aucune instrumentation directe de `SignalTrace`). Preuves : indépendance `PRE_EVENT` (`event_context.phase`, source Mental) vs `RACE_WEEK` (`active_mode`, source Nutrition) — ni l'un ne dérive de l'autre, les deux coexistent sans interférence ; coexistence réelle Technique+Mental+Nutrition (séance DH + Mental AMBER) avec exactement une `TriggeredRule` `MENTAL_AMBER_STRESS`, zéro `TECHNIQUE_*`/`NUTRITION_*` ; extension de la preuve de late-push Mental (docs/06 §V0.3_002C) à une paire appariée GREEN/AMBER avec Technique et Nutrition simultanément actifs des deux côtés — `decision`/`final_session`/`training`/`reasoning`/`override_reason`/`dh_or_technical`/`nutrition`/`recovery` strictement égaux, seuls `mental`/`triggered_rules` diffèrent par exactement une règle ; propriété `MENTAL_RED` (Training) confirmée sur une séance DH réelle (downgrade de charge uniquement, le kind DH survit, Technique/Nutrition suivent le `final_session` stabilisé, aucune seconde règle Mental observable — la preuve directe qu'aucun second `consume()` n'a lieu reste couverte par T12, qui instrumente réellement `SignalTrace`) ; propagation `final_session` + asymétrie `planned_session` acceptée prouvées sur un même fixture réel (`GRIP_WORK` + `arms_grip`/`legs` RED → Training pivote vers `RECOVERY_ACTIVE`, Technique/Nutrition-DH suivent et deviennent inactifs, tandis que la branche force de Nutrition reste active car elle lit intentionnellement le `planned_session` brut, jamais le `final_session`) ; composition jour de course réel (`computeEventContext`, jamais un `EventContext` construit à la main) avec Mental AMBER coexistant, `mental.focus` structurellement absent (la phase ne peut jamais être `PRE_EVENT` quand `in_progress` est vrai) ; Safety dominant une pression maximale (`suspected_concussion` + `RACE_WEEK` + `PRE_EVENT` réel + séance DH planifiée) — `triggered_rules` strictement `[A1]`, les trois sections enrichies exactement `{active:false}` ; aucun nouveau `rule_id` `TECHNIQUE_*`/`NUTRITION_*` à travers l'ensemble des plans combinés ; régression Recovery appariée (mêmes `final_session`/`modeConstraints`/`eventContext.phase` pertinents des deux côtés) alors que Mental/Technique/Nutrition s'activent indépendamment ; déterminisme d'un `DailyPlan` riche multi-domaine. `ENGINE_VERSION` **inchangé** (`head-coach-engine@0.2.0-m1-v0.3_002d`) — aucune sémantique persistée modifiée ; `tests/engineVersion.test.ts` reste l'unique propriétaire, non modifié. **Preuve web : NON EXÉCUTÉE par 002E** — `web/**` était explicitement hors périmètre approuvé ; aucun changement de production/web n'a eu lieu, mais la preuve bout-en-bout que le renderer web existant accepte le `DailyPlan` enrichi B/C/D n'a pas été apportée par ce jalon — ce gate a ensuite été fermé durablement par V0.3_002F. Tests : `npm test` **359/359**, `npm run test:edge` **9/9**, build PASS. Aucune migration, aucun changement web, aucun déploiement remote. Voir `docs/06_ARCHITECTURE.md` §V0.3_002 et `docs/11_DECISION_LOG.md`.
- **V0.3_002F — Rollout remote + clôture finale : CLOSED / REMOTE ROLLOUT COMPLETE (2026-08-30)** : le gate web bout-en-bout reporté par 002E est désormais fermé de façon durable par `web/src/features/dailyPlan/DailyPlanView.enriched.test.tsx` (commit `a59a1bce07a2a85cf5bfc095284c5096800d7803`, déjà présent avant le rollout) — un vrai `RawContext` passé à un vrai `buildDailyPlan` importé par chemin relatif direct depuis `head-coach-engine/src/**` (aucune dépendance/workspace/alias ajouté, aucun objet `DailyPlan` copié à la main) produit un plan réellement Technique+Mental+Nutrition actif, accepté par le vrai `isValidDailyPlan` et rendu par le vrai `DailyPlanView` : `web` **294/294** (28 fichiers), build PASS. Rollout remote : seule `daily-run` redéployée (`npm run deploy:daily-run`, cible `uvolpldwwyvadlamulvr` uniquement — `completed-session`/`get-insights`/`refresh-longitudinal`/`submit-review` non touchées), bundle confirmé incluant `domains/{technique,mental,nutrition}.js` et leurs configs ; post-déploiement `daily-run` **ACTIVE**, version 2, `verify_jwt: true`. Canary fonctionnel remote sur un athlète scratch temporaire uniquement (jamais l'athlète réel de Louis — créé/utilisé/supprimé via `head-coach-engine/tests/supabase/testDb.ts`, le même mécanisme déjà prouvé en M3_006) : `POST /functions/v1/daily-run` → `HTTP 200`, `engine_version: "head-coach-engine@0.2.0-m1-v0.3_002d"`, `dh_or_technical.active`/`mental.active`/`nutrition.active` tous `true`, décision `KEEP` ; relecture de la décision persistée confirme le même `engine_version` (colonne dénormalisée et `daily_plan` JSONB) et les trois sections enrichies actives dans le JSONB persisté — la composition remote et la survie à la persistance sont donc prouvées empiriquement, pas seulement structurellement. Nettoyage scratch complet et vérifié : 0 ligne restante sur `athletes`/`decisions`/`daily_checkins`/`training_blocks`/`planned_sessions`/`health_flags`/`completed_sessions`, utilisateur d'authentification absent. **Écritures sur l'athlète réel : zéro.** Parité de migration reconfirmée en lecture seule avant et après déploiement : 26 locales/26 remote/0 en attente — aucune migration, aucun changement de schéma. Aucun déploiement Vercel effectué ni requis (aucune source `web/**` de production modifiée par V0.3_002). `ENGINE_VERSION` reste `head-coach-engine@0.2.0-m1-v0.3_002d` (aucun bump pour 002E ni 002F — un déploiement n'est pas un changement de sémantique persistée). Rollback non requis ; candidat de rollback d'urgence audité et confirmé valide : `b9a39dfe39d1c07116c85f4b41cc74f0b8387b85` (parent immédiat pré-002B, wrapper M3 + moteur pré-002B + script de déploiement tous présents). **V0.3_002 — Domain Coaching Enrichment dans son ensemble est COMPLETE (2026-08-30)** : les 6 jalons de la décomposition verrouillée (002A→002F) sont tous clos, `daily-run` sert l'engine enrichi en production, aucune régression locale/remote détectée. Voir `docs/06_ARCHITECTURE.md` §V0.3_002 et `docs/11_DECISION_LOG.md`.

### IN PROGRESS

- **V0.3_003 — Planning / Session Intent** : V0.3_003A CLOSED / ARCHITECTURE LOCKED (2026-08-31), V0.3_003B (data-access/write path) CLOSED LOCALLY (2026-08-31), **V0.3_003C (web planning workflow) CLOSED LOCALLY (2026-08-31)** — implémentation + durcissement (`5c66385`, `feat: add V0.3_003C web planning workflow`) poussés sur `origin/main`. `web/src/pages/PlanPage.tsx` + `web/src/features/planning/{PlanningDayCard,planningKindGroups}.ts(x)` (nouveaux) et `web/src/{App.tsx,components/AppNav.tsx,lib/date.ts}` (modifiés) : route authentifiée `/plan` (`RequireAuth`, entrée `AppNav` "Plan"), horizon aujourd'hui→J+6 (7 jours), sélecteur natif `<select>`/`<optgroup>` sur les 15 kinds plannables (`RACE_ACTIVITY` jamais exposé), 11 kinds à charge variable exigeant un choix explicite HEAVY/MODERATE/LIGHT (aucun défaut), 4 kinds à charge fixe sans sélecteur de charge, libellés français réutilisés depuis `TRAINING_KIND_LABELS`/`LOAD_PROFILE_LABELS` (aucune duplication). `PlanPage` reste l'unique source de vérité en mémoire des rows persistées pour les 7 dates ; `PlanningDayCard` ne porte que son état de brouillon/édition — un brouillon non sauvegardé n'est jamais affiché comme persisté, une mutation échouée laisse l'éditeur ouvert et l'affichage persisté inchangé avec un message sûr. Row legacy `intervention=NULL` gérée sans jamais fabriquer une intention riche (label coarse réutilisé depuis `SESSION_TYPE_LABELS`, sélection explicite requise pour remplacer). Durcissement course contre la montre asynchrone inter-jours : une mutation qui se résout après que l'athlète a déjà ouvert un autre jour ne referme plus cet autre jour. `npm test` par défaut reste self-contained : **473 PASS / 9 SKIPPED** (482 total, 35 fichiers) ; suite `web` complète avec opt-in **482/482 PASS**. Moteur strictement inchangé (`head-coach-engine` **359/359**, edge **9/9**, `ENGINE_VERSION` inchangé). Aucune migration, aucun déploiement remote (Supabase ou Vercel) — `/plan` est implémenté et committé localement, **pas déployé en production**. Intégration `/today` reste **V0.3_003D — NOT STARTED**. Décomposition : V0.3_003A (CLOSED) → V0.3_003B (CLOSED LOCALLY) → V0.3_003C (CLOSED LOCALLY) → V0.3_003D (Today integration + e2e, NEXT) → V0.3_003E (rollout production). Voir `docs/06_ARCHITECTURE.md` §V0.3_003 et `docs/11_DECISION_LOG.md`.

### NOT STARTED

- **V0.3_003D/E** — architecture verrouillée en V0.3_003A ; V0.3_003B/C (data-access/write path, web planning workflow) CLOSED LOCALLY, voir section IN PROGRESS ci-dessus.
- **ActiveExperiment (T9)** — explicitement différé, pas annulé : redevient le prochain candidat concret après clôture de V0.3_003 (voir `docs/11_DECISION_LOG.md`).
- **Autres enrichissements P1+** (domaine Contexte pro, domaine Analyse, LLM couche E, planificateur hebdomadaire *automatique* — distinct de V0.3_003's planning manuel, intégrations externes) — aucun jalon concret sélectionné/architecturé à ce stade ; voir `docs/12_BACKLOG.md` §P1/§P2.

## Prochaines étapes (ordre)

1. ~~**Implémenter M2** (Claude Code) : audit DDL → migrations additives → DAL → adapter → `computeDailyFor` / `runDailyFor` → tests A/B/C/D.~~ **DONE (local), 2026-08-16.**
2. ~~**Review Louis M2 → déploiement remote séparé.**~~ **DONE, 2026-08-17.**
3. ~~**M3 — Edge Function locale** (`daily-run`) : portabilité Deno, boundary auth, branchement moteur.~~ **DONE (local), 2026-08-17 (M3_001/M3_002/M3_003).**
4. ~~**M3 remote** : canary de packaging `--use-api` (M3_005), déploiement réel de `daily-run` + run authentifié complet prouvé (M3_006).~~ **DONE (remote), 2026-08-18.**
5. ~~**M4 — première interface utilisable** (Today screen / check-in, historique, déploiement production HTTPS).~~ **DONE, 2026-08-19.**
6. ~~**M5_001-M5_006B** — timeline longitudinal, calculateur d'outcomes, détecteurs recommandation-vs-exécution et sommeil-énergie, ledger d'evidence append-only + cycle de vie.~~ **M5_006B CLOSED, 2026-08-26.** Voir `docs/11_DECISION_LOG.md`.
7. ~~**M5_006C** — détecteur de persistance de la douleur.~~ **CLOSED, 2026-08-27** (implémentation locale uniquement, aucune migration). Voir `docs/11_DECISION_LOG.md`.
8. ~~**M5_006D** — agrégation déterministe d'evidence (`pattern_evidence_current_effective`).~~ **CLOSED, 2026-08-27** (implémentation locale uniquement, aucune migration). Voir `docs/11_DECISION_LOG.md`.
9. ~~**M5_007** — insights / revue humaine.~~ **CLOSED, 2026-08-27** (schéma/RPC de revue humaine déployé sur `uvolpldwwyvadlamulvr` ; moteur d'insight implémenté/testé localement, non invoqué automatiquement en production). Voir `docs/11_DECISION_LOG.md`. **M5 dans son ensemble : COMPLETE.**
10. ~~**V0.3_001A** — correction de runtime + orchestration (`refresh-longitudinal`, `get-insights`, correction d'identité `recommendation_vs_actual_execution`).~~ **CLOSED LOCALLY, 2026-08-28** (aucun déploiement remote). Voir `docs/11_DECISION_LOG.md`.
11. ~~**V0.3_001B** — backfill + durcissement complet (remote).~~ **CLOSED REMOTE, 2026-08-28** (déploiement + premier backfill + preuve d'idempotence remote, tous vérifiés). Voir `docs/11_DECISION_LOG.md`.
12. ~~**V0.3_001C** — API de revue canonique + surface web Insights.~~ **CLOSED REMOTE, 2026-08-28** (`submit-review` déployé ACTIVE v1, surface web `/insights` déployée en production, écriture de revue réelle non exécutée faute de candidat naturel). Voir `docs/11_DECISION_LOG.md`. **V0.3_001 dans son ensemble : COMPLETE.**
13. **V0.3_002A** — architecture + contrats de V0.3_002 (Domain Coaching Enrichment) verrouillés. **CLOSED / ARCHITECTURE LOCKED, 2026-08-28** (documentation uniquement, aucun code). Voir `docs/11_DECISION_LOG.md`.
14. **V0.3_002B** — domaine Technique DH. **CLOSED LOCALLY, 2026-08-28** (`b28e013f`/`e9573dd4`, tests 275/275 + edge 9/9, aucun déploiement remote). Voir `docs/11_DECISION_LOG.md`.
15. **V0.3_002C** — domaine Mental. **CLOSED LOCALLY, 2026-08-29** (`06e87888`/`3b1753b4`, tests 310/310 + edge 9/9, aucun déploiement remote). Voir `docs/11_DECISION_LOG.md`.
16. **V0.3_002D** — domaine Nutrition. **CLOSED LOCALLY, 2026-08-30** (`eac17586`/`792608c6`, tests 347/347 + edge 9/9, aucun déploiement remote). Voir `docs/11_DECISION_LOG.md`.
17. **V0.3_002E** — intégration/régressions cross-domaine. **CLOSED LOCALLY, 2026-08-30** (`fa8d1c74`, test-only, tests 359/359 + edge 9/9, `ENGINE_VERSION` inchangé, aucun déploiement remote ; preuve web reportée comme gate 002F). Voir `docs/11_DECISION_LOG.md`.
18. **V0.3_002F** — rollout remote + clôture finale. **CLOSED / REMOTE ROLLOUT COMPLETE, 2026-08-30** (gate web fermé durablement par `a59a1bc`, `daily-run` seule redéployée sur `uvolpldwwyvadlamulvr` ACTIVE/`verify_jwt:true`, canary scratch confirmant Technique/Mental/Nutrition actifs + `engine_version` `_002d` remote + persistance, nettoyage complet, migration 26/26 inchangée, aucun déploiement Vercel). **V0.3_002 dans son ensemble : COMPLETE.** Voir `docs/11_DECISION_LOG.md`.
19. **V0.3_003A** — architecture verrouillée pour Planning/Session Intent. **CLOSED / ARCHITECTURE LOCKED, 2026-08-31** (documentation uniquement, aucun code). Voir `docs/11_DECISION_LOG.md`.
20. **V0.3_003B** — data-access/write path pour Planning/Session Intent. **CLOSED LOCALLY, 2026-08-31** (`1b11750`/`9ed4fa6`/`5ee6dfc`, web 415 pass/9 skip par défaut + 18/18 fichier d'intégration Planning avec opt-in (9 RLS + 9 garde cible locale) + 424/424 opt-in complet, engine 359/359 + edge 9/9, aucune migration, aucun déploiement remote). Voir `docs/11_DECISION_LOG.md`.
21. **V0.3_003C** — web planning workflow (`/plan`) pour Planning/Session Intent. **CLOSED LOCALLY, 2026-08-31** (`5c66385`, web 473 pass/9 skip par défaut + 482/482 opt-in complet, engine 359/359 + edge 9/9, aucune migration, aucun déploiement remote). Voir `docs/11_DECISION_LOG.md`.

## Prochain milestone

**V0.3_002 — Domain Coaching Enrichment : COMPLETE (2026-08-30). V0.3_003 — Planning / Session Intent : IN PROGRESS (depuis 2026-08-31).** V0.3_003A (architecture) **CLOSED**, V0.3_003B (data-access/write path) **CLOSED LOCALLY**, V0.3_003C (web planning workflow) **CLOSED LOCALLY (2026-08-31)**. Prochain jalon concret : **V0.3_003D — Today integration + e2e regressions**, portée verrouillée dans `docs/06_ARCHITECTURE.md` §V0.3_003. `docs/12_BACKLOG.md` §P1/§P2 liste les autres catégories d'enrichissement candidates (`ActiveExperiment` reste le candidat suivant après V0.3_003, différé mais non annulé), à titre indicatif.

Statut M2 : **DONE (local + remote), 2026-08-17.**
Statut M3 : **DONE (local + remote), 2026-08-18.**
Statut M4 : **COMPLETE, 2026-08-19.**
Statut M5 : **COMPLETE, 2026-08-27** — M5_006B **CLOSED (local + remote), 2026-08-26** ; M5_006C **CLOSED (local uniquement, aucune migration), 2026-08-27** ; M5_006D **CLOSED (local uniquement, aucune migration), 2026-08-27** ; M5_007 **CLOSED (local + remote), 2026-08-27**. Détail milestone par milestone dans `docs/11_DECISION_LOG.md`.
Statut V0.3_001 : **COMPLETE, 2026-08-28** — V0.3_001A **CLOSED LOCALLY** ; V0.3_001B **CLOSED REMOTE** (déploiement + premier backfill + idempotence remote vérifiés, implémentation runtime-aligned HEAD `27fcb564dff2558b1a1dafc81c914d46a8957011`) ; V0.3_001C **CLOSED REMOTE** (`submit-review` déployé ACTIVE v1 + surface web `/insights` en production ; écriture de revue réelle non exécutée en remote — zéro candidat naturel, aucune donnée synthétique créée). Détail dans `docs/11_DECISION_LOG.md`.
Statut V0.3_002 : **COMPLETE, 2026-08-30** — V0.3_002A **CLOSED / ARCHITECTURE LOCKED** (2026-08-28, documentation uniquement) ; V0.3_002B (Technique DH) **CLOSED LOCALLY** (2026-08-28, `b28e013f`/`e9573dd4`, tests 275/275 + edge 9/9) ; V0.3_002C (Mental) **CLOSED LOCALLY** (2026-08-29, `06e87888`/`3b1753b4`, tests 310/310 + edge 9/9) ; V0.3_002D (Nutrition) **CLOSED LOCALLY** (2026-08-30, `eac17586`/`792608c6`, tests 347/347 + edge 9/9) ; V0.3_002E (intégration/régressions, test-only) **CLOSED LOCALLY** (2026-08-30, `fa8d1c74`, tests 359/359 + edge 9/9) ; V0.3_002F (rollout remote/clôture) **CLOSED / REMOTE ROLLOUT COMPLETE** (2026-08-30, gate web durable `a59a1bc` — 294/294 —, `daily-run` seule redéployée ACTIVE/`verify_jwt:true` sur `uvolpldwwyvadlamulvr`, canary scratch confirmant `engine_version` `_002d` + Technique/Mental/Nutrition actifs remotely + persistance, migration 26/26 inchangée, zéro déploiement Vercel, zéro écriture sur l'athlète réel). Détail dans `docs/06_ARCHITECTURE.md` §V0.3_002 et `docs/11_DECISION_LOG.md`.
Statut V0.3_003 : **IN PROGRESS, depuis 2026-08-31** — V0.3_003A **CLOSED / ARCHITECTURE LOCKED** (2026-08-31) ; V0.3_003B (data-access/write path) **CLOSED LOCALLY** (2026-08-31, `1b11750`/`9ed4fa6`/`5ee6dfc`, web 415 pass/9 skip par défaut + 18/18 fichier d'intégration Planning avec opt-in (9 RLS + 9 garde cible locale) + 424/424 opt-in complet, engine 359/359 + edge 9/9, aucune migration, aucun déploiement remote) ; V0.3_003C (web planning workflow) **CLOSED LOCALLY** (2026-08-31, `5c66385`, route `/plan` authentifiée + entrée AppNav, horizon 7 jours, web 473 pass/9 skip par défaut + 482/482 opt-in complet, engine 359/359 + edge 9/9, aucune migration, aucun déploiement remote — `/plan` implémenté et committé localement, pas encore déployé en production) ; V0.3_003D (Today integration + e2e) **NOT STARTED — prochain** ; V0.3_003E (rollout production) **NOT STARTED**. Aucune migration, aucun changement `head-coach-engine/src/**`, aucun changement `DailyPlan`, aucun bump `ENGINE_VERSION` attendus sur l'ensemble du jalon. Détail dans `docs/06_ARCHITECTURE.md` §V0.3_003 et `docs/11_DECISION_LOG.md`.

Critères de sortie M2 :
- [x] Audit initial du DDL réel (tables + enums touchés par M2) réalisé et tracé (2026-08-14).
- [x] Baseline V0.2 capturée via `supabase db dump --linked --schema public` en `supabase/migrations/20260814095000_baseline_v0_2.sql` (timestamp réel de capture), fichier read-only strict.
- [x] Migrations DB `M2_001`, `M2_002` (incluant enum `confidence_level`), `M2_003`, `M2_004` (REQUIRED), `M2_005`, `M2_006` appliquées sur l'instance Supabase locale, dans l'ordre canonique après la baseline. Tous timestamps de nom strictement postérieurs à celui de la baseline.
- [x] Index unique partiel PostgreSQL sur `health_flags` empêchant deux flags ouverts `(athlete_id, flag_type)` simultanés (colonne réelle `flag_type` confirmée par audit), tout en autorisant un nouveau flag après résolution.
- [x] Fonction PostgreSQL `persist_daily_run` (via RPC) : deux écritures dans un seul appel (transaction unique implicite), `SECURITY INVOKER`, `EXECUTE` réservé au rôle serveur M2, jamais exposée à `PUBLIC`/`anon`/`authenticated`. Aucune logique de coaching côté SQL.
- [x] DAL minimal (repositories + adapter) implémenté dans `src/supabase/`.
- [x] Séparation calcul (`computeDailyFor`) / persistance (`runDailyFor`).
- [x] `buildRawContext` **rejette** un checkin courant M2 dont un des trois critères douleur (`pain_traumatic`, `pain_function_loss`, `pain_getting_worse`) est `NULL` — jamais de conversion silencieuse en `false`.
- [x] Inversion `DbSessionType → TrainingIntervention` limitée aux mappings mathématiquement non ambigus (`REST`, `BIKE_MAINTENANCE`, `RACE_PREP`). Autres cas legacy → `planned_session = null` + warning.
- [x] `decisions` append-only (aucune contrainte d'unicité, aucun upsert destructif). `confidence_level` obligatoire via DAL pour toute nouvelle décision M2 ; `confidence` numeric legacy reste `NULL`. `overridden_by_user` conserve son default DB `false`.
- [x] 75/75 tests M1 toujours verts (moteur strictement non modifié).
- [x] Tests M2 A/B/C/D verts (voir `docs/10_TEST_PLAN.md` §M2).
- [x] Cycle A1/A2/A3/A4 → A5 (jour N → jour N+1) explicitement prouvé par test d'intégration, y compris résolution du flag et A1 répétée sans doublon.
- [x] Équivalence fixture ↔ Supabase prouvée pour tous les scénarios M1 canoniques (19 scénarios).
- [x] Aucun `db push`, aucun `migration repair`, aucune modification remote pendant tout le développement local.
- [x] Aucune modification de `src/{types,engine,rules,domains,mapping}`.
- [x] Update `00_PROJECT_STATUS.md` avec M2 DONE (local + remote) — ce document, 2026-08-17.
- [x] Revue Louis (local + tests, avant tout push remote).
- [x] **Uniquement après review** : baseline V0.2 marquée comme déjà appliquée dans l'historique remote via `supabase migration repair` (une seule fois, hors développement), puis premier `supabase db push` M2 vers la DB Louis — effectué 2026-08-17 sur `uvolpldwwyvadlamulvr`. Post-deploy audit PASS (M2_001→M2_006, RPC `persist_daily_run` permissions, données existantes préservées).
- [x] Commit / push des sources M2 dans le repo (aucun secret).
- [x] Décision Go/No-Go M3 (Louis) : Go, 2026-08-17.

Critères de sortie M3 (local, DONE) :
- [x] M3_001 — frontière de build Deno prouvée : import source `.ts` échoue (attendu), import `dist/*.js` compilé fonctionne, aucune duplication du moteur, `dist/` reste gitignored.
- [x] M3_002 — boundary d'authentification prouvée : JWT utilisateur → `ctx.supabase`/RLS → athlete propre uniquement (jamais `body.athlete_id`), isolation cross-user prouvée, zéro écriture métier.
- [x] M3_003 — moteur réellement branché : `ctx.supabaseAdmin` → `runDailyFor(admin, athlete.id, date)` (athlete.id exclusivement RLS-dérivé), réponse `{dailyPlan, decisionId, healthFlagId, warnings}`, mapping d'erreurs typées M1/M2 → HTTP stable, aucune fuite de donnée sensible.
- [x] `npm test` = 226/226 (dont 75/75 M1), `npm run test:edge` = 9/9, `npm run test:m3:http` = 26/26 (répété avec succès, cleanup vérifié).
- [x] Aucune migration DB, aucun changement `head-coach-engine/src/{types,engine,rules,domains,mapping,supabase}`.
- [x] Commit / push M3_001, M3_002, M3_003 dans le repo (aucun secret).

Critères de sortie M3 (remote, DONE 2026-08-18) :
- [x] Canary de packaging `supabase functions deploy --use-api` validée sur `uvolpldwwyvadlamulvr` (M3_005) : graphe transitif `dist/**` uploadé automatiquement, module chargé remote (`{"ok":true,"runDailyForLoaded":true}`), canary supprimée.
- [x] Déploiement remote de `daily-run` (M3_006), `verify_jwt: true`, statut `ACTIVE`.
- [x] Run authentifié réel complet prouvé : 401 (sans auth / JWT invalide), 422 `no_checkin_for_date` avec zéro écriture, 200 avec `DailyPlan` réel après checkin neutre, decision persistée et vérifiée (id/athlete_id/`daily_plan` deep-equal/`confidence` NULL/`confidence_level` renseigné), réponse sans fuite, isolation RLS via athlete propre.
- [x] Tous les fixtures scratch remote (user, athlete, training_block, checkin) supprimés et vérifiés absents ; aucune donnée réelle de Louis touchée.
- [x] Aucune valeur de clé/secret affichée, loguée ou commitée ; fichiers temporaires supprimés.

Critères de sortie M4 (COMPLETE, 2026-08-19) :
- [x] `web/` bootstrap (React 19, Vite, TypeScript strict, Tailwind v4, React Router 7, Vitest/RTL), authentification Supabase (`AuthContext`/`RequireAuth`/`LoginPage`, résolution athlete via RLS uniquement).
- [x] `/today` : date locale sans biais UTC, check-in quotidien (CRUD RLS-scopé, champs santé/douleur tri-state `boolean | null` jamais coercés en `false`), génération et rendu production du `DailyPlan` réel via `daily-run`.
- [x] `/history` + `/history/:decisionId` : lecture seule des décisions persistées (`decisions.daily_plan`), aucune recomputation, dégradation propre sur une ligne au shape legacy, aucun signal santé déduit côté frontend.
- [x] **Correctif de sécurité** : `decisions` rendue réellement append-only — policy `decisions_own_data` (`FOR ALL`) remplacée par `decisions_own_select` (`SELECT`-only pour `authenticated`), privilèges d'écriture directs révoqués (`authenticated`/`anon`), `persist_daily_run`/`service_role` reste l'unique chemin d'écriture. Prouvé empiriquement en local puis sur `uvolpldwwyvadlamulvr` (INSERT/UPDATE/DELETE directs → `permission denied`).
- [x] Polish mobile : cibles tactiles ≥44px, zoom automatique iOS évité (inputs ≥16px), navigation deux-onglets, aucune régression de hiérarchie produit.
- [x] Déploiement production HTTPS sur Vercel : projet/scope dédiés `louis-performance-system` (jamais `graviacoach`), rewrite SPA (`web/vercel.json`) pour les deep-links `/today`/`/history`/`/history/:id`, variables d'environnement browser-safe uniquement (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`).
- [x] Supabase Auth Site URL mise à jour sur `uvolpldwwyvadlamulvr` pour `https://louis-performance-system.vercel.app`.
- [x] Smoke authentifié réel sur le téléphone de Louis : login, `/today`, check-in, `/history`, détail, refresh direct, logout/relogin — tous PASS.
- [x] `npm test` = 163/163, `npm run build` = PASS.

## Règle

**Ne pas marquer une étape comme `DONE` avant qu'elle existe réellement dans Git.**
